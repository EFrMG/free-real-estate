# Copy to .env for local development
# Vite only exposes variables prefixed with `VITE_`, and inlines them into the build
# set the real value in a host's build-time environment in production

# Base URL of the deployed backend API, no trailing slash
# It defaults to http://localhost:3000 when unset
VITE_API_URL=http://localhost:3000
